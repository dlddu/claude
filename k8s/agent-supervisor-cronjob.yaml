apiVersion: batch/v1
kind: CronJob
metadata:
  name: agent-supervisor
  labels:
    app: agent-supervisor
    component: cronjob
spec:
  schedule: "*/10 * * * *"  # Every 10 minutes
  concurrencyPolicy: Allow
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            app: agent-supervisor
            component: job
        spec:
          restartPolicy: Never
          containers:
            - name: agent-supervisor
              image: ghcr.io/dlddu/claude:latest
              imagePullPolicy: Always
              command:
                - /bin/bash
                - -c
                - /app/.claude/scripts/agent-supervisor.sh
              env:
                - name: LINEAR_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: agent-supervisor-secrets
                      key: LINEAR_API_KEY
                - name: CLAUDE_CODE_OAUTH_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: agent-supervisor-secrets
                      key: CLAUDE_CODE_OAUTH_TOKEN
                - name: AWS_ACCESS_KEY_ID
                  valueFrom:
                    secretKeyRef:
                      name: agent-supervisor-secrets
                      key: AWS_ACCESS_KEY_ID
                      optional: true
                - name: AWS_SECRET_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: agent-supervisor-secrets
                      key: AWS_SECRET_ACCESS_KEY
                      optional: true
                - name: AWS_REGION
                  valueFrom:
                    secretKeyRef:
                      name: agent-supervisor-secrets
                      key: AWS_REGION
                      optional: true
                - name: AWS_DYNAMODB_TABLE_NAME
                  valueFrom:
                    secretKeyRef:
                      name: agent-supervisor-secrets
                      key: AWS_DYNAMODB_TABLE_NAME
                      optional: true
